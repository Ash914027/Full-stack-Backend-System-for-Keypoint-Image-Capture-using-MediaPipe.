# Full-stack-Backend-System-for-Keypoint-Image-Capture-using-MediaPipe.
Develop a backend system that: 1. Extracts keypoints from images using MediaPipe. 2. Stores keypoints in an SQL database (e.g., PostgreSQL or MySQL) 3. Stores the original image in a NoSQL database (e.g., MongoDB) 4. Provides a REST API for full CRUD. 5. Uses a Cron job to zip all data daily. 6. 
